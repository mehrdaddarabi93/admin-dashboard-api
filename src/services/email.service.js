/**
 * سرویس ارسال ایمیل
 *
 * چرا این فایل جدا از بقیه سرویس‌هاست؟
 * چون منطق "چطور ایمیل بفرستیم" را از منطق تجاری (مثلاً بازیابی
 * رمز عبور) جدا می‌کند. اگر فردا بخواهیم به‌جای چاپ در کنسول، از
 * Gmail یا SendGrid واقعی استفاده کنیم، فقط همین فایل تغییر می‌کند
 * و auth.service.js دست‌نخورده می‌ماند.
 *
 * در حال حاضر (محیط توسعه) ایمیل واقعی ارسال نمی‌شود؛ فقط محتوای
 * آن در کنسول سرور چاپ می‌شود تا بدون نیاز به تنظیمات SMTP، فرآیند
 * تست قابل انجام باشد.
 */

/**
 * ارسال ایمیل بازیابی رمز عبور
 *
 * @param {string} toEmail - ایمیل مقصد
 * @param {string} resetUrl - لینک کامل بازیابی رمز (شامل توکن)
 */
const sendPasswordResetEmail = async (toEmail, resetUrl) => {
  /**
   * TODO: در production این بخش باید با Nodemailer یا یک سرویس
   * واقعی (SendGrid, Mailgun, ...) جایگزین شود.
   */
  console.log("\n📧 ============ ایمیل بازیابی رمز عبور ============");
  console.log(`گیرنده: ${toEmail}`);
  console.log(`لینک بازیابی: ${resetUrl}`);
  console.log(`این لینک تا 15 دقیقه دیگر معتبر است.`);
  console.log("===================================================\n");
};

module.exports = {
  sendPasswordResetEmail,
};
